name: Dependency audit

on:
  schedule:
    - cron: "17 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inherited-lnbits-runtime:
    name: LNbits ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: stable-v1.5.6
            ref: v1.5.6
          - name: dev-a16dd7c
            ref: a16dd7cee3b89785c69f08f41462e7a2cecb62d3
    steps:
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install the LNbits runtime
        env:
          LNBITS_REF: ${{ matrix.ref }}
        run: |
          python -m venv .audit-venv
          .audit-venv/bin/python -m pip install --upgrade pip pip-audit
          .audit-venv/bin/python -m pip install \
            "lnbits @ git+https://github.com/lnbits/lnbits.git@${LNBITS_REF}"
          .audit-venv/bin/python -m pip check
          .audit-venv/bin/python -m pip freeze | grep -Ei \
            '^(lnbits|pillow|pyjwt|starlette)=='
      - name: Audit the complete inherited runtime
        run: |
          .audit-venv/bin/python -m pip_audit \
            --strict --format json --output inherited-runtime-audit.json
      - name: Preserve the machine-readable audit evidence
        if: ${{ always() && hashFiles('inherited-runtime-audit.json') != '' }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: inherited-runtime-audit-${{ matrix.name }}
          path: inherited-runtime-audit.json
          if-no-files-found: error
          retention-days: 30
